[phases.setup]
nixPkgs = ["chromium", "playwright-driver", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt", "playwright install chromium"]

[start]
cmd = "cd backend && python3 -m uvicorn main:app --host 0.0.0.0 --port $PORT"
